<!DOCTYPE html>
<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <title>Perfil de Usuario</title>
    <link
      href="https://cdnjs.cloudflare.com/ajax/libs/materialize/1.0.0/css/materialize.min.css"
      rel="stylesheet"
    />
    <link
      href="https://fonts.googleapis.com/icon?family=Material+Icons"
      rel="stylesheet"
    />
  </head>

  <body class="grey lighten-4">
    <nav class="blue darken-2">
      <div class="nav-wrapper container">
        <a href="/dashboard" class="brand-logo">Dashboard</a>
        <ul id="nav-desktop" class="right">
          <% if (role === 'admin') { %>
          <li><a href="/dashboardAdmin">Usuarios</a></li>
          <% } %> <% if (role === 'user' || role === 'admin') { %>
          <li><a href="/profile">Perfil</a></li>
          <li><a href="#">Cerrar sesión</a></li>
          <% } %>
        </ul>
      </div>
    </nav>

    <main class="container" style="margin-top: 60px">
      <div class="card z-depth-2">
        <div class="card-content center-align">
          <span class="card-title blue-text text-darken-2">
            <i class="material-icons left">person</i>
            Perfil de Usuario
          </span>
          <p class="grey-text">
            Aquí puedes ver y editar tu información personal.
          </p>
        </div>
      </div>

      <div class="row" style="margin-top: 40px">
        <div class="col s6 offset-s3">
          <div class="card hoverable">
            <div class="card-content">
              <span class="card-title">
                <i class="material-icons left">account_circle</i>Datos
                Personales
              </span>
              <ul class="collection">
                <li class="collection-item">
                  <strong>Nombre:</strong> <%= user.name || '' %> <%= user.lastName || '' %>
                </li>
                <li class="collection-item">
                  <strong>Email:</strong> <%= user.email || 'Sin correo' %>
                </li>
                <li class="collection-item">
                  <strong>Teléfono:</strong> <%= user.phoneNumber || 'No registrado' %>
                </li>
                <li class="collection-item">
                  <strong>Dirección:</strong> <%= user.adress || 'No registrada' %>
                </li>
                <li class="collection-item">
                  <strong>Fecha de nacimiento:</strong> <%= user.birthDate ? user.birthDate.toISOString().slice(0,10) : 'No registrada' %>
                </li>
              </ul>
            </div>
            <div class="card-action center-align">
              <a href="/profile/edit" class="btn blue darken-2 waves-effect">
                <i class="material-icons left">edit</i> Editar Perfil
              </a>
            </div>
          </div>
        </div>
      </div>
    </main>

    <footer class="page-footer blue darken-3">
      <div class="container center-align">
        © <%= new Date().getFullYear() %> Mi App | Todos los derechos reservados
      </div>
    </footer>

    <script src="https://cdnjs.cloudflare.com/ajax/libs/materialize/1.0.0/js/materialize.min.js"></script>
  </body>
</html>
